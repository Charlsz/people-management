## infra

Esta carpeta sirve para todo lo que arranca la aplicación completa: Docker Compose, variables de entorno de ejemplo y scripts de inicialización.

#### Que se encuentra aqui:

docker-compose.yml: frontend, Postgres, servicios y n8n.

.env.example: variables ejemplo.

postgres/init.sql: si quieremos inicializar la BD automáticamente.